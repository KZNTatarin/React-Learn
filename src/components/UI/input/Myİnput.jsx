import React from 'react';
import classes from './Myİnput.module.css'

const Myİnput = (props) => {
    return (
        <input className={classes.myİnput} {...props}/>
    );
};

export default Myİnput;

